# JourneyOS Showcase needs NO secrets and makes NO outbound network calls.
# This file exists to document that surface explicitly.

# API listen URL (default used by launchSettings / Dockerfile):
# ASPNETCORE_URLS=http://localhost:5100

# Frontend → API base (only needed when NOT using the Vite dev proxy):
# VITE_API_BASE=http://localhost:5100
